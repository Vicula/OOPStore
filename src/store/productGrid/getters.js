export default {
  getGridProducts: (s) => {
    // // let p = secDecrypt(productGrid.products.getProds())
    // // let r = {}
    // // Object.keys(p).map((k) =>{
    // //   o.map((v)=>{
    // //     p[k][v.key].includes(v.val) ? r[k] = p[k] : ()=>{return}
    // //   })
    // // })
    // return productGrid.safeProducts
    console.log(s.safeProducts)
    return s.safeProducts
  },
}
